<% @page_title="記事内容の編集"%>

<h1><%= @page_title %></h1>
<div class="link_right">
    <%= link_to "詳細画面に戻る", @study %>
</div>

<%= form_for @study do |form| %>
    <%= render "memo", form: form %>
    <div class="submit_button"><%= form.submit %></div>
<% end %>
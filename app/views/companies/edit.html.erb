<% @page_title="企業情報の編集"%>

<h1><%= @page_title %></h1>
<div class="link_right">
    <%= link_to "詳細画面に戻る", @company %>
</div>

<%= form_for @company do |form| %>
    <%= render "form", form: form %>
    <div class="submit_button"><%= form.submit %></div>
<% end %>